# fashion-mnist-mlp
A PyTorch-based Multi-Layer Perceptron (MLP) for Fashion-MNIST classification. Includes three different experimental setups to compare the impact of hidden units and activation functions on model accuracy.
